# OpenVINO 改动前的核心线程信息获取、核心绑定和优先级配置逻辑

## 概述

本文档整理了在用户改动之前，OpenVINO 在推理时如何获取核心线程信息、进行核心绑定以及配置优先级的完整逻辑。

## 1. 核心线程信息获取 (`get_proc_type_table`)

### 1.1 函数位置
- **文件**: `src/inference/src/system_conf.cpp`
- **改动前实现**:
```cpp
std::vector<std::vector<int>> get_proc_type_table() {
    CPU& cpu = cpu_info();
    std::lock_guard<std::mutex> lock{cpu._cpu_mutex};
    return cpu._proc_type_table;
}
```

### 1.2 逻辑说明
- **作用**: 返回 CPU 处理器类型表，包含系统中所有 CPU 核心的类型信息
- **数据结构**: `_proc_type_table` 是一个二维向量，每一行代表一个 NUMA 节点或 socket 的 CPU 信息
  - `[0]`: 所有处理器总数 (ALL_PROC)
  - `[1]`: 主核心数 (MAIN_CORE_PROC)
  - `[2]`: 高效核心数 (EFFICIENT_CORE_PROC)
  - `[3]`: 超线程核心数 (HYPER_THREADING_PROC)
  - `[4]`: NUMA 节点 ID (PROC_NUMA_NODE_ID)
  - `[5]`: Socket ID (PROC_SOCKET_ID)
- **线程安全**: 使用互斥锁保护，确保多线程访问安全
- **数据来源**: `cpu._proc_type_table` 在系统初始化时通过解析 `/proc/cpuinfo` 等系统文件填充

### 1.3 使用场景
- 在 `IStreamsExecutor::Config::update_executor_config()` 中调用，用于：
  - 确定可用的 CPU 核心数量
  - 根据核心类型（大核/小核）分配线程
  - 计算每个 stream 应该使用的核心数

## 2. 核心绑定逻辑 (`reserve_available_cpus`)

### 2.1 调用流程

#### 2.1.1 入口点
- **位置**: `src/inference/src/dev/threading/istreams_executor.cpp::update_executor_config()`
- **改动前代码**:
```cpp
if (_cpu_pinning || _cpu_reservation) {
    reserve_available_cpus(_streams_info_table, _stream_processor_ids, 
                          _cpu_reservation ? CPU_USED : NOT_USED);
}
```

#### 2.1.2 核心函数
- **函数**: `reserve_available_cpus()`
- **位置**: `src/inference/src/system_conf.cpp`
- **改动前实现**:
```cpp
void reserve_available_cpus(const std::vector<std::vector<int>> streams_info_table,
                            std::vector<std::vector<int>>& stream_processors,
                            const int cpu_status) {
    CPU& cpu = cpu_info();
    std::lock_guard<std::mutex> lock{cpu._cpu_mutex};
    
    ov::threading::reserve_cpu_by_streams_info(streams_info_table,
                                               cpu._numa_nodes,
                                               cpu._cpu_mapping_table,
                                               cpu._proc_type_table,
                                               stream_processors,
                                               cpu_status);
}
```

### 2.2 `reserve_cpu_by_streams_info` 详细逻辑

#### 2.2.1 函数位置
- **文件**: `src/inference/src/dev/threading/cpu_streams_executor_internal.cpp`

#### 2.2.2 核心算法流程

1. **解析 streams_info_table**
   - 遍历 `_streams_info_table`，提取每个 stream 的需求：
     - 处理器类型 (PROC_TYPE)
     - NUMA 节点 ID (STREAM_NUMA_NODE_ID)
     - Socket ID (STREAM_SOCKET_ID)
     - 每个 stream 的线程数 (THREADS_PER_STREAM)
     - Stream 数量 (NUMBER_OF_STREAMS)

2. **构建匹配条件**
   - 为每个 stream 创建匹配条件字符串：`proc_type + numa_node + socket`
   - 例如：`"1" + "0" + "0"` 表示主核心、NUMA 0、Socket 0

3. **遍历 CPU 映射表**
   - 遍历 `_cpu_mapping_table`，查找未使用的 CPU (`CPU_MAP_USED_FLAG == NOT_USED`)
   - 对每个 CPU，构建其标识字符串：`core_type + numa_node_id + socket_id`

4. **匹配和分配**
   - 将 CPU 标识与 stream 条件匹配
   - 如果匹配成功：
     - 将 CPU ID 添加到对应 stream 的 `_stream_processors` 中
     - 标记 CPU 为已使用 (`CPU_MAP_USED_FLAG = cpu_status`)
     - 当某个 stream 分配够所需线程数时，移动到下一个 stream

5. **更新处理器类型表**
   - 如果 `cpu_status > NOT_USED`，调用 `update_proc_type_table()` 更新可用核心信息

#### 2.2.3 关键数据结构

**streams_info_table 结构**:
```cpp
streams_info_table[i] = {
    NUMBER_OF_STREAMS,      // 该类型的 stream 数量
    PROC_TYPE,              // 处理器类型
    THREADS_PER_STREAM,     // 每个 stream 的线程数
    STREAM_NUMA_NODE_ID,    // NUMA 节点 ID
    STREAM_SOCKET_ID,       // Socket ID
    ...
}
```

**cpu_mapping_table 结构**:
```cpp
cpu_mapping_table[i] = {
    CPU_MAP_PROCESSOR_ID,   // 逻辑 CPU ID
    CPU_MAP_CORE_TYPE,      // 核心类型
    CPU_MAP_NUMA_NODE_ID,   // NUMA 节点 ID
    CPU_MAP_SOCKET_ID,      // Socket ID
    CPU_MAP_USED_FLAG,      // 使用标志
    ...
}
```

**stream_processors 输出**:
```cpp
stream_processors[stream_id] = {cpu_id1, cpu_id2, ...}  // 每个 stream 绑定的 CPU ID 列表
```

## 3. 线程绑定到核心 (`pin_thread_to_vacant_core`)

### 3.1 函数位置
- **文件**: `src/inference/src/dev/threading/thread_affinity.cpp`

### 3.2 改动前实现逻辑

```cpp
bool pin_thread_to_vacant_core(int thrIdx,
                               int hyperthreads,
                               int ncores,
                               const CpuSet& procMask,
                               const std::vector<int>& cpu_ids) {
    if (procMask == nullptr)
        return false;
    const size_t size = CPU_ALLOC_SIZE(ncores);
    const int num_cpus = CPU_COUNT_S(size, procMask.get());
    thrIdx %= num_cpus;  // 限制在可用 CPU 范围内
    
    int mapped_idx;
    if (cpu_ids.size() > 0) {
        // 如果提供了 cpu_ids，直接使用
        mapped_idx = cpu_ids[thrIdx];
    } else {
        // 否则使用轮询方式分配
        int cpu_idx = 0;
        for (int i = 0, offset = 0; i < thrIdx; ++i) {
            cpu_idx += hyperthreads;
            if (cpu_idx >= num_cpus)
                cpu_idx = ++offset;
        }
        
        // 在 procMask 中找到第 cpu_idx 个设置为 1 的位
        mapped_idx = -1;
        while (cpu_idx >= 0) {
            mapped_idx++;
            if (CPU_ISSET_S(mapped_idx, size, procMask.get()))
                --cpu_idx;
        }
    }
    
    // 创建目标 CPU 掩码并绑定线程
    CpuSet targetMask{CPU_ALLOC(ncores)};
    CPU_ZERO_S(size, targetMask.get());
    CPU_SET_S(mapped_idx, size, targetMask.get());
    bool res = pin_current_thread_by_mask(ncores, targetMask);
    return res;
}
```

### 3.3 逻辑说明

1. **参数说明**:
   - `thrIdx`: 线程索引
   - `hyperthreads`: 超线程步长（通常为 1 或 2）
   - `ncores`: CPU 核心总数
   - `procMask`: 进程可用的 CPU 掩码
   - `cpu_ids`: 预分配的 CPU ID 列表（可选）

2. **两种绑定模式**:
   - **模式 1**: 如果提供了 `cpu_ids`，直接使用 `cpu_ids[thrIdx]` 作为目标 CPU
   - **模式 2**: 如果没有提供 `cpu_ids`，使用轮询算法：
     - 按照 `hyperthreads` 步长分配
     - 当超出范围时，从下一个物理核心开始

3. **实际绑定**:
   - 创建只包含目标 CPU 的掩码
   - 调用 `pin_current_thread_by_mask()` 执行实际的线程绑定（Linux 下使用 `sched_setaffinity`）

### 3.4 调用时机

在 `CPUStreamsExecutor::Impl::Stream::Observer::on_scheduler_entry()` 中调用：

```cpp
void on_scheduler_entry(bool) override {
    pin_thread_to_vacant_core(tbb::this_task_arena::current_thread_index(),
                            _threadBindingStep,
                            _ncpus,
                            _mask,
                            _cpu_ids);  // 这里传入的是 stream 分配的 CPU IDs
}
```

**关键点**:
- `_cpu_ids` 来自 `_impl->_config.get_stream_processor_ids()[stream_id]`
- 这个列表是在 `reserve_available_cpus()` 中为每个 stream 分配的 CPU ID 列表
- TBB 线程池中的每个线程在进入任务调度器时，会调用此函数绑定到对应的 CPU 核心

## 4. 优先级配置

### 4.1 改动前状态
**改动前 OpenVINO 没有线程优先级配置功能**。

- `Observer::on_scheduler_entry()` 中只进行核心绑定，没有优先级设置
- 线程使用系统默认的调度策略和优先级

## 5. 完整执行流程

### 5.1 初始化阶段

1. **系统信息收集**
   ```
   cpu_info() 初始化
   ├── 解析 /proc/cpuinfo
   ├── 构建 _cpu_mapping_table
   ├── 构建 _proc_type_table
   └── 确定 NUMA 节点和 Socket 信息
   ```

2. **配置更新**
   ```
   IStreamsExecutor::Config::update_executor_config()
   ├── 调用 get_proc_type_table() 获取 CPU 信息
   ├── 根据配置计算 streams_info_table
   └── 如果启用 CPU pinning，调用 reserve_available_cpus()
   ```

3. **CPU 分配**
   ```
   reserve_available_cpus()
   ├── 调用 reserve_cpu_by_streams_info()
   ├── 遍历 CPU 映射表，匹配 stream 需求
   ├── 为每个 stream 分配 CPU IDs
   └── 更新 stream_processor_ids
   ```

### 5.2 运行时阶段

1. **Stream 创建**
   ```
   CPUStreamsExecutor::Impl::Stream 构造函数
   ├── 获取 stream_id
   ├── 确定 NUMA 节点
   └── 如果启用 pinning，调用 init_stream()
   ```

2. **TBB Arena 和 Observer 设置**
   ```
   init_stream()
   ├── 调用 get_cur_stream_info() 确定 stream 类型
   │   └── stream_type 决定绑定方式:
   │       ├── STREAM_WITH_OBSERVE: 使用 Observer 进行线程级绑定（需要 cpu_pinning=true）
   │       ├── STREAM_WITH_NUMA_ID: 使用 TBB 的 NUMA 约束
   │       ├── STREAM_WITH_CORE_TYPE: 使用 TBB 的核心类型约束
   │       └── STREAM_WITHOUT_PARAM: 不使用任何约束
   ├── 调用 create_tbb_task_arena()
   │   ├── 如果 stream_type == STREAM_WITH_OBSERVE:
   │   │   ├── 获取所有 stream 的 CPU IDs: _config.get_stream_processor_ids()
   │   │   ├── 根据 stream_id 选择: stream_processors[stream_id]
   │   │   ├── 创建普通 TBB task_arena
   │   │   └── 创建 Observer，传入 _cpu_ids = stream_processors[stream_id]
   │   ├── 如果 stream_type == STREAM_WITH_NUMA_ID:
   │   │   └── 创建带 NUMA 约束的 TBB task_arena
   │   ├── 如果 stream_type == STREAM_WITH_CORE_TYPE:
   │   │   └── 创建带核心类型约束的 TBB task_arena
   │   └── 否则:
   │       └── 创建普通 TBB task_arena（无约束）
   └── Observer 开始观察 TBB arena（如果创建了 Observer）
   ```

3. **线程绑定**
   ```
   TBB 线程进入任务调度器
   ├── Observer::on_scheduler_entry() 被调用
   ├── 调用 pin_thread_to_vacant_core()
   │   ├── 使用 stream 分配的 _cpu_ids[thread_index]
   │   └── 绑定线程到对应 CPU 核心
   └── 线程执行推理任务
   ```

4. **线程退出**
   ```
   TBB 线程退出任务调度器
   └── Observer::on_scheduler_exit() 被调用
       └── 恢复进程级别的 CPU 掩码
   ```

## 6. Stream 类型决定逻辑

### 6.1 `get_cur_stream_info()` 中的 stream_type 决定

在 `cpu_streams_executor_internal.cpp` 中，`get_cur_stream_info()` 函数根据以下条件决定 stream 类型：

```cpp
if (pinning) {  // cpu_pinning == true
    stream_type = STREAM_WITH_OBSERVE;  // 使用 Observer 进行线程级绑定
} else {
    stream_type = STREAM_WITHOUT_PARAM;
    // 混合核心机器且指定了核心类型（非 ALL_PROC）
    if (proc_type_table[0][EFFICIENT_CORE_PROC] > 0 && core_type != ALL_PROC) {
        stream_type = STREAM_WITH_CORE_TYPE;  // 使用 TBB 核心类型约束
    } 
    // 混合核心机器，使用所有核心但未使用小核
    else if (proc_type_table[0][EFFICIENT_CORE_PROC] > 0 && core_type == ALL_PROC && !ecore_used) {
        stream_type = STREAM_WITH_CORE_TYPE;
        core_type = MAIN_CORE_PROC;
    } 
    // 多 NUMA 节点
    else if (proc_type_table.size() > 1 && numa_node_id >= 0) {
        stream_type = STREAM_WITH_NUMA_ID;  // 使用 TBB NUMA 约束
    }
}
```

### 6.2 不同 stream_type 的绑定方式

| stream_type | 绑定方式 | 说明 |
|------------|---------|------|
| `STREAM_WITH_OBSERVE` | Observer + `pin_thread_to_vacant_core()` | 精确的线程级 CPU 绑定，需要 `cpu_pinning=true` |
| `STREAM_WITH_NUMA_ID` | TBB task_arena NUMA 约束 | 依赖 TBB 的 NUMA 感知，不进行精确绑定 |
| `STREAM_WITH_CORE_TYPE` | TBB task_arena 核心类型约束 | 依赖 TBB 的核心类型感知，不进行精确绑定 |
| `STREAM_WITHOUT_PARAM` | 无约束 | 完全依赖操作系统调度 |

**关键点**: 只有 `STREAM_WITH_OBSERVE` 类型才会使用 `reserve_available_cpus()` 分配的 CPU IDs 进行精确绑定。

## 7. 关键设计点

### 6.1 CPU 分配策略
- **按类型匹配**: 根据 stream 的处理器类型需求（大核/小核）分配
- **NUMA 感知**: 考虑 NUMA 节点，尽量在同一节点内分配
- **Socket 感知**: 考虑 Socket 信息，优化跨 Socket 访问

### 6.2 线程绑定策略
- **Stream 级别绑定**: 每个 stream 绑定到一组 CPU 核心
- **线程级别绑定**: Stream 内的每个线程绑定到该 stream 分配的核心之一
- **轮询分配**: 如果没有明确指定，使用轮询方式在 stream 的核心列表中分配

### 7.3 数据流
```
get_proc_type_table()
    ↓
update_executor_config()
    ↓
reserve_available_cpus()
    ↓
reserve_cpu_by_streams_info()
    ↓
_config._stream_processor_ids[stream_id] = {cpu_id1, cpu_id2, ...}
    ↓
Stream::init_stream()
    ↓
create_tbb_task_arena(stream_id, ...)
    ├── stream_processors = _config.get_stream_processor_ids()
    └── _cpu_ids = stream_processors[stream_id]
    ↓
Observer::Observer(..., _cpu_ids)
    ↓
Observer::on_scheduler_entry()
    ↓
pin_thread_to_vacant_core(thread_index, ..., _cpu_ids)
    ↓
线程绑定到 _cpu_ids[thread_index % _cpu_ids.size()]
```

## 8. 改动前的限制

1. **无法手动指定核心绑定**: 只能依赖 OpenVINO 的自动分配算法
2. **无优先级配置**: 线程使用系统默认优先级
3. **核心数量检测依赖系统**: 如果系统报告的核心数不准确（如容器环境），可能导致分配错误

## 9. 相关文件清单

- `src/inference/src/system_conf.cpp`: CPU 信息获取和核心分配
- `src/inference/src/dev/threading/istreams_executor.cpp`: Stream 配置更新
- `src/inference/src/dev/threading/cpu_streams_executor_internal.cpp`: CPU 分配核心算法
- `src/inference/src/dev/threading/cpu_streams_executor.cpp`: Stream 执行器和线程绑定
- `src/inference/src/dev/threading/thread_affinity.cpp`: 线程绑定实现

## 10. 总结

### 10.1 核心流程概览

改动前的 OpenVINO 核心绑定逻辑遵循以下流程：

1. **系统信息收集**: 通过 `get_proc_type_table()` 获取 CPU 核心信息
2. **Stream 配置**: 根据用户配置和系统信息，计算每个 stream 需要的核心类型和数量
3. **CPU 分配**: 通过 `reserve_available_cpus()` 自动为每个 stream 分配 CPU 核心
4. **线程绑定**: 在 TBB 线程进入任务调度器时，通过 Observer 机制将线程绑定到分配的 CPU 核心

### 10.2 关键特点

- **自动分配**: CPU 核心由 OpenVINO 根据系统信息和配置自动分配
- **类型感知**: 考虑大核/小核、NUMA 节点、Socket 等特性
- **精确绑定**: 使用 Linux `sched_setaffinity` 进行线程级精确绑定
- **无优先级控制**: 线程使用系统默认优先级

### 10.3 改动前的核心限制

1. **无法手动指定核心**: 用户无法通过环境变量等方式手动指定核心绑定
2. **无优先级配置**: 无法设置线程优先级（如 SCHED_FIFO）
3. **系统依赖**: 核心数量检测完全依赖系统报告，在容器等受限环境中可能不准确

这些限制正是用户后续改动要解决的问题。

